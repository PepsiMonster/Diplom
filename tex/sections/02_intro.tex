\label{ch:model_and_methods}

В данной главе формулируется математическая модель исследуемой ресурсной системы массового обслуживания с интенсивностями, зависящими от состояния. Описываются выбранные семейства распределений времени обслуживания и входящего потока, а также вводится набор ключевых метрик для оценки чувствительности системы. Кроме того, детально рассматривается алгоритм имитационного моделирования, архитектура разработанного программного комплекса и методика верификации полученных результатов.

\section{Формальная постановка ресурсной СМО}
\label{sec:formal_model}

% Что здесь писать:
% 1. Описание системы: N серверов, общий ресурс R. 
% 2. Правила поступления заявок: как заявка занимает прибор и ресурс. 
% 3. Условие отказа (loss condition): нет свободных серверов ИЛИ не хватает свободного ресурса.
% 4. Ввод функций \lambda(n) и \mu(n) (state-dependent профиль).
% 5. Важно использовать формулы:
% \begin{equation}
% \label{eq:loss_cond}
%   ...
% \end{equation}
% и ссылаться на них по тексту как (в формуле \ref{eq:loss_cond}).


\section{Семейства распределений обслуживания и входного потока}
\label{sec:distributions}

% Что здесь писать:
% 1. Входящий поток (Arrival Process): Пуассоновский (как базовый), Эрланг-2, Эрланг-4, Гиперэкспоненциальный. Описать их математически (плотности, коэффициенты вариации).
% 2. Время обслуживания (Workload): Экспоненциальное (базлайн), Детерминированное, Эрланг (2, 4, 8), Гиперэкспоненциальное (H2, Heavy).
% 3. Обосновать, почему выбраны именно они (покрытие диапазона коэффициента вариации CV от 0 до >1).


\section{Алгоритм имитационного моделирования}
\label{sec:algorithm}

% Что здесь писать:
% 1. Обоснование выбора дискретно-событийного моделирования (DES).
% 2. Структура событий (поступление заявки, завершение обслуживания).
% 3. Логика изменения состояния при state-dependent: как пересчитываются таймеры событий при изменении \lambda(n) и \mu(n) (это очень важный технический момент!).
% 4. Вычислительная сложность алгоритма и структуры данных (очередь с приоритетами / heap).


\section{Архитектура программного комплекса}
\label{sec:architecture}

% Что здесь писать:
% 1. Разделение на вычислительное ядро (Rust) и аналитическую обертку (Python).
% 2. Схема работы: генерация конфигов (JSON) -> параллельные прогоны (Rust) -> сбор suite_result.json -> визуализация (Pandas/Matplotlib).
% 3. Обеспечение воспроизводимости: фиксация random seed для каждого прогона (репликации).
% Примечание: Если есть блок-схема работы программы, вставляй её сюда:
% \begin{figure}[H]
%     \centering
%     \includegraphics[width=0.8\textwidth]{architecture_scheme}
%     \caption{Архитектура имитационного комплекса}
%     \label{fig:architecture}
% \end{figure}


\section{Исследуемые метрики и показатели чувствительности}
\label{sec:metrics}

% Что здесь писать:
% 1. Определение стационарных метрик: вероятность потери ($P_{loss}$), пропускная способность ($Throughput$), среднее число заявок ($\mathbb{E}[N]$), средний занятый ресурс ($\mathbb{E}[\gamma]$).
% 2. Метрика стационарного распределения $\pi(k)$.
% 3. Формализация понятия «чувствительность». Как мы ее измеряем? (Относительное отклонение $\Delta$ в процентах относительно марковского базлайна).


\section{Верификация модели и проверка корректности реализации}
\label{sec:verification}

% Что здесь писать:
% 1. Sanity checks: почему мы верим симулятору? 
% 2. Сравнение результатов симулятора на частных, аналитически разрешимых случаях (например, классическая M/M/N/0 без учета ресурса) с точными формулами Эрланга.
% 3. Анализ сходимости (уменьшение доверительных интервалов при увеличении числа репликаций).


\section*{Выводы по главе 2}
\addcontentsline{toc}{section}{Выводы по главе 2}

В данной главе была формализована математическая модель... (краткий параграф-выжимка). 
Разработанный на базе языков Rust и Python программный комплекс позволяет проводить масштабные вычислительные эксперименты с высокой производительностью и гарантированной воспроизводимостью. Верификация на частных случаях подтвердила математическую корректность симулятора. Введенный аппарат метрик и дизайна эксперимента дает необходимую основу для проведения сравнительного анализа, результаты которого будут подробно рассмотрены в Главе 3.